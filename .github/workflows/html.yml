# name: Deploy to GitHub Pages

# on:
#   push:
#     branches:
#       - main

# permissions:
#   contents: write

# jobs:
#   deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Setup Pages
#         uses: actions/configure-pages@v4

#       - name: Upload HTML content
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: .

#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4
#       - name: Setup Pages
#         uses: actions/configure-pages@v4
#         with:
#           enablement: true  # ✅ Automatically enables Pages
name: Deploy HTML to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write   # ✅ This line is important!

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload HTML
        uses: actions/upload-pages-artifact@v2
        with:
          path: .

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
